#ifndef ARRAYS_H
#define ARRAYS_H

#include <stdio.h>
#include <stdlib.h>
#include <time.h>

void read_arrays(int **, int *, int, char **);
void print_arrays(int **, int, char **);
void lin_sort(int *, int);
void merge(int **, int, const char *);

#endif

